# L'environnement d'entraînement, figé pour Python 3.9 (celui de macOS).
#   python3 -m venv ml/.venv && ml/.venv/bin/pip install -r ml/requirements.txt
numpy<2
pandas==2.0.3
scikit-learn==1.3.2
xgboost==2.0.3
shap==0.44.1
