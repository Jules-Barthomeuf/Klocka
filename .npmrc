# Le SDK Claude Agent exige @anthropic-ai/sdk >= 0.93 alors que Klocka est sur 0.32 :
# les deux cohabitent, npm doit juste ne pas refuser. Sans cette ligne, un
# npm install sans --legacy-peer-deps echoue chez l'hebergeur et rien ne s'installe.
legacy-peer-deps=true
